import React from 'react'

const MovieCard = () => {
    return (
        <div>
            <h1>{MovieName}</h1>
            <p>{MovieDescription}</p>
        </div>
    )
}

export default MovieCard
